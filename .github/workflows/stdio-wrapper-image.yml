name: Build and Push STDIO Wrapper Image

on:
  workflow_call:
  workflow_dispatch:
  push:
    tags:
      - "v*"

concurrency:
  group: publish-stdio-wrapper-image
  cancel-in-progress: false

permissions:
  contents: read
  packages: write
  security-events: write
  id-token: write

jobs:
  generate-matrix:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    outputs:
      images: ${{ steps.images.outputs.value }}
    steps:
      - uses: actions/checkout@v4
      - name: Read STDIO wrapper image
        id: images
        run: |
          images=$(yq eval -o=json '[.images[] | select(.group == "stdio-wrapper") | del(.group)]' repository-images.yaml | jq -c .)
          echo "value=$images" >> "$GITHUB_OUTPUT"

  publish:
    needs: generate-matrix
    uses: ./.github/workflows/repository-images.yml
    with:
      images_json: ${{ needs.generate-matrix.outputs.images }}
    secrets: inherit
